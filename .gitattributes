# Normalize line endings to LF for all text files (batch/cmd override below)
* text=auto eol=lf

# Windows-specific scripts use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

cfml/src/parser.c linguist-vendored

# Zig bindings
build.zig linguist-generated
build.zig.zon linguist-generated
