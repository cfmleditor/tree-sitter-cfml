<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>io.github.cfmleditor</groupId>
  <artifactId>tree-sitter-cfml</artifactId>
  <name>TreeSitter CFML</name>
  <version>0.26.31</version>
  <description>CFML grammar for tree-sitter</description>
  <url>https://github.com/cfmleditor/tree-sitter-cfml</url>
  <licenses>
    <license>
      <name>MIT</name>
      <url>https://spdx.org/licenses/MIT.html</url>
    </license>
  </licenses>
  <developers>
    <developer>
      <name>Gareth Edwards</name>
      <email>ghedwards@gmail.com</email>
    </developer>
    <developer>
      <name>Gavin Baumanis</name>
      <email>gavinbaumanis@gmail.com</email>
    </developer>
  </developers>
  <scm>
    <url>https://github.com/cfmleditor/tree-sitter-cfml</url>
    <connection>scm:git:git://github.com/cfmleditor/tree-sitter-cfml.git</connection>
    <developerConnection>scm:git:ssh://github.com/cfmleditor/tree-sitter-cfml.git</developerConnection>
  </scm>
  <properties>
    <maven.compiler.release>23</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.deploy.skip>true</maven.deploy.skip>
    <gpg.skip>true</gpg.skip>
    <publish.auto>false</publish.auto>
    <publish.skip>true</publish.skip>
    <!--
      Where the tests look for the native libraries, so that a checkout can be
      tested without installing anything system-wide. `npm run build:native`
      writes all four (the runtime plus the three grammars) to build/native;
      `make` writes the three grammars into their own dialect directories but
      does not build the runtime.
    -->
    <native.library.path>${project.basedir}/build/native${path.separator}${project.basedir}/cfml${path.separator}${project.basedir}/cfscript${path.separator}${project.basedir}/cfquery</native.library.path>
  </properties>
  <dependencies>
    <dependency>
      <groupId>io.github.tree-sitter</groupId>
      <artifactId>jtreesitter</artifactId>
      <version>0.26.1</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>6.0.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <sourceDirectory>bindings/java/main</sourceDirectory>
    <testSourceDirectory>bindings/java/test</testSourceDirectory>
    <plugins>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.4</version>
        <configuration>
          <reportsDirectory>
            ${project.build.directory}/reports/surefire
          </reportsDirectory>
          <argLine>--enable-native-access=ALL-UNNAMED -Djava.library.path=${native.library.path}</argLine>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.12.0</version>
        <executions>
          <execution>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <show>public</show>
          <nohelp>true</nohelp>
          <noqualifier>true</noqualifier>
          <doclint>all,-missing</doclint>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-gpg-plugin</artifactId>
        <version>3.2.8</version>
        <executions>
          <execution>
            <phase>verify</phase>
            <goals>
              <goal>sign</goal>
            </goals>
            <configuration>
              <bestPractices>true</bestPractices>
              <gpgArguments>
                <arg>--no-tty</arg>
                <arg>--pinentry-mode</arg>
                <arg>loopback</arg>
              </gpgArguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>io.github.mavenplugins</groupId>
        <artifactId>central-publishing-maven-plugin</artifactId>
        <version>1.1.1</version>
        <executions>
          <execution>
            <phase>deploy</phase>
            <goals>
              <goal>publish</goal>
            </goals>
            <configuration>
              <waitUntil>validated</waitUntil>
              <autoPublish>${publish.auto}</autoPublish>
              <skipPublishing>${publish.skip}</skipPublishing>
              <outputFilename>${project.artifactId}-${project.version}.zip</outputFilename>
              <deploymentName>${project.artifactId}-${project.version}.zip</deploymentName>
            </configuration>
          </execution>
        </executions>
        <extensions>true</extensions>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>release</id>
      <properties>
        <gpg.skip>false</gpg.skip>
        <publish.auto>true</publish.auto>
        <publish.skip>false</publish.skip>
      </properties>
    </profile>
  </profiles>
</project>
